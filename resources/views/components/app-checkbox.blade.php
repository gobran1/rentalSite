<div class="{{$containerClass}}">
    <input
        type="{{$type}}"
        name="{{$name}}"
        value="{{$value}}"
        id="{{$name}}_{{$value}}"
        class="{{$inputClass}}"
        style="float: left"
    >

    <label
        class="{{$labelClass}}"
        for="{{$name}}_{{$value}}"

    >
        {{$label}}
    </label>
</div>

